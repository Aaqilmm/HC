import type{Rule}from"../types.js";import{tokenize,replaceRanges}from"../syntax.js";export const assignmentSpacingRule:Rule={id:"07-assignment-spacing",name:"Assignment spacing inconsistency",apply(code,c){const ts=tokenize(code,c.language),sig=ts.filter(x=>!(["ws","newline","comment"]as string[]).includes(x.kind)),rs:any[]=[];for(let i=1;i<sig.length-1;i++){const t=sig[i];if(t.kind!=="op"||t.text!=="=")continue;const l=sig[i-1],r=sig[i+1];if(l.line!==t.line||r.line!==t.line||(c.rng??Math.random)()>.35)continue;const before=code.slice(l.end,t.start),after=code.slice(t.end,r.start);const choices=["="," =","= "," = "];const pick=choices[Math.floor((c.rng??Math.random)()*choices.length)];rs.push({start:l.end,end:r.start,text:before||after?pick:"="});}return replaceRanges(code,rs);}};
