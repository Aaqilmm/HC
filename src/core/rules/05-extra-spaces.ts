import type {Rule} from "../types.js";import {tokenize,replaceRanges} from "../syntax.js";export const extraSpacesRule:Rule={id:"05-extra-spaces",name:"3-5 percent extra-space probability",apply(code,c){const ts=tokenize(code,c.language),rs:any[]=[];for(let i=1;i<ts.length-1;i++){const t=ts[i];if(t.kind!=="ws"||!/^[ ]+$/.test(t.text))continue;const p=ts[i-1],n=ts[i+1];if(p.line!==t.line||n.line!==t.line||["(","[",".","->","::"].includes(p.text)||[")","]",",",";",":"].includes(n.text))continue;if((c.rng??Math.random)()<.04)rs.push({start:t.end,end:t.end,text:" "});}return replaceRanges(code,rs);}};
